import styled from 'styled-components'

export const  RoomWrapper = styled.div`
color: #222;
.room-list {
        display: flex;
        flex-direction: row;
        align-items: center;
        flex-wrap: wrap;
        margin: 0 -8px;
      }
`